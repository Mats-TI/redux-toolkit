const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "trialbon_user",
      password: "root123",
      database: "trialbon_internify",
    },
    listPerPage: 10,
  };
  module.exports = config;