const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "digivars_dbuser",
      password: "}+]4)03HM[e]",
      database: "digivars_react",
    },
    listPerPage: 10,
  };
  module.exports = config;